# 从理论到部署：仿真、真实部署与论文回填实施计划

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 关联论文 | 《认证之为结构：DAG-based BFT 共识中去认证的隐性代价与延迟优势的条件性》(`final/final_paper.md`) |
| 文档目的 | 把论文的理论讨论落地为可运行的本地仿真与真实部署，**并将结果回填论文** |
| 创建日期 | 2026-05-21 |
| 末次更新 | 2026-05-22 |
| 当前状态 | 阶段一未启动；`simpy` skill 已安装；设计决策已确认 |

### 0.1 已确认的设计决策（2026-05-22）

| 决策点 | 选定方案 |
|---|---|
| 敌手模型范围 | **先只建等价（equivocation）攻击**，"引用不给内容""晚绑定排序操纵"后续迭代 |
| 变体实现顺序 | **先把 uncertified 变体跑通**，再补 certified 变体 |
| 网络延迟模型 | **指定分布**的延迟（非常数、非均匀区间） |

---

## 1. 背景与动机

### 1.1 论文是什么

这是一篇**纯理论/分析型**论文。中心论点：DAG-BFT 中"去认证"（certified → uncertified）不是可分离的性能优化，而是**结构性替换**——认证承载的三个不变量（非等价性、数据可用性、因果历史完整性）未消失，只是被重新分配；隐性代价集中在唯一的"物理型"不变量**数据可用性**上，且是一笔**或有负债**；uncertified 的延迟优势是**条件性**的，由成本模型 `净优势 ≈ Δ_save − ρ·Δ_recover` 刻画。

### 1.2 现有代码的局限

现有 `stage2_write/code/dag_bft_model.py` 只是上述闭式公式的**实现核对**——Monte Carlo 均值收敛到解析期望（论文 §7.2 自己承认这只验证"公式实现正确"，不构成"贴合真实协议"的证据）。它本身声明"不是对任何真实协议的实测"。

### 1.3 待填补的缺口（全部出自论文自身）

| 论文出处 | 论文承认的缺口 |
|---|---|
| §9「模型抽象的代价」 | 成本模型**忽略了排队效应、恢复请求间的相互干扰** |
| §7.4 | ρ 与 Δ_recover 在拥塞反馈下**相互耦合**，闭式模型未刻画 |
| §7.7 边界情形 4 | "ρ 中等、网络中等时定性模型给不出结论，应回退到受控实测" |
| §10 未来工作之二 | "设计**跨协议的受控实证**，定量标定 Δ_save、Δ_recover、ρ" |
| §3.5 | 真实的 minimal pair（同骨架认证版 vs 去认证版）**严格意义上无法构造** |
| §8.5 | MEV / "晚绑定"排序操纵攻击仅给出草图，"未量化，留作未来工作" |

---

## 2. 总目标与策略

### 2.1 总目标（最高优先级原则）

> **本计划的全部产出最终服务于论文。** 仿真与真实部署不是独立工程项目，而是为论文补足实证维度的手段；**所有仿真与部署结果都必须回填进论文**（见 §2.4 与阶段三）。任何实现决策若与"能否、如何回填论文"冲突，以回填需求为准。

据此，总目标为：
1. 用受控仿真把 Δ_save / Δ_recover / ρ 从**假设的输入参数**变成**被测量的输出量**；
2. 用真实协议本地实测校准仿真；
3. 将上述结果系统性地回填论文，使论文从"纯理论分析"升级为"理论分析 + 受控实证"。

### 2.2 为什么"仿真先行"——关键论证

直接部署真实 Bullshark/Mysticeti 不能作为首选，理由出自论文 §3.5：真实的 uncertified 协议**从来不是"某协议减认证"**，而是"减认证再加一批补偿机制"，十几个维度同时变。在真实代码库上测，只能得到混淆后的总效果，恰好落回论文批判的 SubRQ0 陷阱。**受控变量隔离（minimal pair）只能在自己控制的仿真器里成立。** 因此真实部署的角色是**校准/佐证仿真**，而非替代。

### 2.3 三阶段概览

- **阶段一 — SimPy 本地仿真**：核心价值所在，立即启动。
- **阶段二 — 真实部署**：仿真稳定后渐进启动，用真实实测校准阶段一参数。
- **阶段三 — 结果回填论文**：把阶段一、二的结果系统性写入论文；可在阶段一结束后先做一轮、阶段二结束后再做一轮（增量回填）。

> ⚠️ **审批关卡**：阶段一、二可连续推进；但**进入阶段三（回填论文）前必须停止，取得用户明确批准后方可动笔修改论文**。未获批准不得改动 `final/final_paper.md` 或在 `stage8_paper_integration/` 生成修订稿。回填材料包（发现摘要）可在获批前准备，作为审批的呈递材料。

### 2.4 结果回填论文映射表

每一项实证结果都预先指定其在论文中的落点。**实现仿真/部署时必须保留可追溯性**——每个输出量都对应下表某一行。

| 结果来源 | 回填论文位置 | 改动性质 |
|---|---|---|
| 阶段一：构造出受控 minimal pair | §3.5、§5.5 | §3.5 现称"严格意义上往往无法构造"——补注"在受控仿真环境中可构造，本工作已实现" |
| 阶段一：Figure 1/2 实测版 | §7.2、§7.6（图 1、图 2） | 解析图旁增配仿真实测图，或改为"解析 + 实测对照图" |
| 阶段一：排队 / 耦合效应定量结果 | §7.4、§9「模型抽象的代价」 | §9 该条局限由"未刻画"改写为"已由仿真定量刻画"；§7.4 拥塞反馈获仿真支撑 |
| 阶段一：§7.7 边界情形 4 的仿真结论 | §7.7 | 边界情形 4 现称"给不出确定结论"——若仿真能给出，须改写该条 |
| 阶段一+二：Δ_save / Δ_recover / ρ 标定值 | §7.1–7.2 成本台账 | 参数从"说明性数值"升级为"仿真/实测标定值" |
| 阶段二：跨协议受控实证 | §10 未来工作之二 | 该未来工作项部分完成——从"未来工作"上移为"已完成工作"，或新增章节承载 |
| 阶段一+二：整体 | §9 数据可用性声明、AI 使用声明；摘要 | 论文性质从"纯理论"变为"理论+实证"，相关声明须同步更新 |

**两个诚实边界（回填时必须守住）**：
1. 阶段一仿真仍是"模型的数值图示"，它是比 Monte Carlo 更丰富的**实现核对**，**不**等于"贴合真实协议"的证据——后者只能由阶段二真实实测提供。回填论文时须沿用 §7.2 已有的这一区分，不可越界宣称。
2. 论文 §1.4 的"诚实声明"与抗断章框架必须保留；新增实证结果同样标注网络/敌手模型前提。

### 2.5 回填材料的留存规范

阶段一、二执行过程中，每一项**拟回填**的结果须**即时留存**为可复现、可追溯的三件套：

1. **原始数据**——每次扫参/实测的原始输出（CSV/JSON），须含完整运行参数：N、f、延迟分布及其参数、随机种子、轮数、trial 数。存于 `stage6_simulation/data/`（阶段一）、`stage7_deployment/data/`（阶段二）。
2. **图表**——每张拟入论文的图同时存 `.png`（预览）与 `.pdf`（300 DPI，LaTeX 嵌入），沿用论文既有图表规范（见 `stage2_write/code/README.md`）。存于各阶段的 `figures/`。
3. **溯源元数据**——每个数据文件/图配一条记录，注明：对应 §2.4 映射表哪一行、支持或挑战论文哪条主张、生成脚本与版本、生成日期、属"仿真"还是"真实实测"。各阶段 `data/` 下用一份 `manifest`（JSON 或 `留存清单.md`）汇总。

留存原则：
- **即时留存**——结果一产出即存盘并登记 manifest，不留到阶段末补;
- **可复现**——凭保存的种子与参数，任何图都能用记录的命令重跑;
- **不覆盖**——扫参迭代产生新结果时另存（带日期/版本号），旧版本不删，便于回填时对比与回溯;
- **诚实标注随数据走**——数据文件与 manifest 注明"仿真"或"真实实测"，防止回填时把两者混淆。

§4.9 / §5.5 的"回填材料包" = 发现摘要 + 上述三件套的索引；它是阶段三审批关卡的呈递材料。

---

## 3. 设计原则

1. **以不变量为模块边界**——继承论文 §3.2 方法论：仿真器模块按"维持某个不变量的机制"切分，显式区分「非等价性模块」「数据可用性模块」「因果历史完整性模块」。模块边界对照论文，结论才能直接回填。
2. **受控 minimal pair**——certified 版与 uncertified 版共享同一骨架，**仅在认证强度这一个变量上不同**，其余配置完全一致。
3. **参数即输出**——Δ_save、Δ_recover、ρ 不作为输入硬编码，而是从仿真运行中测量得到。
4. **退化可验证**——关闭排队/耦合效应后，仿真结果必须收敛到现有闭式模型，作为正确性回归基线。
5. **诚实标注**——所有图表与结论标明"仿真"性质，区分"实现核对"与"贴合真实协议的证据"。
6. **面向回填的可追溯性**——每个仿真/实测输出量须标注它对应 §2.4 映射表的哪一行、支持或挑战论文的哪一条主张。无法回填的输出不产出。
7. **即时留存**——关键数据与图表一经产出即按 §2.5 规范存盘并登记 manifest，不依赖事后补做。

---

## 4. 阶段一：SimPy 本地仿真

### 4.0 概述

- **技术栈**：Python ≥ 3.9，`simpy`（离散事件仿真）、`numpy`、`matplotlib`。
- **代码位置**：新目录 `stage6_simulation/`，不改动 `stage2_write/code/` 原始产物。
- **用到的 skill**：`simpy`（已安装）。

### 4.1 步骤 1.1 — 仿真骨架

- **目标**：N 个验证者 = SimPy 进程，按轮（round）推进；消息经 SimPy 事件传递，附网络延迟。
- **实现要点**：验证者进程、轮次时钟、消息信道抽象；**网络延迟采用指定分布**（决策 3）——延迟从一个可配置的概率分布采样（如对数正态/伽马，参数后续可由阶段二实测标定），而非常数或均匀区间。
- **验收**：无故障、无敌手下能稳定推进 ≥ 2000 轮并完成提交。
- **论文锚点**：§6.1 模型设定（n≥3f+1、quorum=n−f）。

### 4.2 步骤 1.2 — 共享资源与排队

- **目标**：把"带宽"建模为 SimPy 共享资源，使排队与恢复请求间的相互干扰**自动涌现**。
- **实现要点**：`simpy.Resource` / `simpy.Container` 表示链路或节点带宽；恢复请求与正常广播竞争同一资源。
- **验收**：注入恢复负载后能观测到排队延迟随负载非线性上升。
- **论文锚点**：§9「忽略排队与相互干扰」、§7.4 拥塞反馈正反馈。

### 4.3 步骤 1.3 — uncertified 变体（先行）

- **目标**：先实现 **uncertified 变体**并跑通（决策 2）——best-effort 广播 + 提交时可用性检查 + reconciliation（skip / retry）。
- **实现要点**：认证逻辑与补偿逻辑做成可插拔模块，骨架其余部分为后续 certified 变体共享；本步只接入补偿逻辑。
- **验收**：uncertified 变体能在无敌手下稳定提交，并在数据缺失时触发 reconciliation。
- **论文锚点**：§5.4 削弱+补偿、§5.5 提交路径对照（uncertified 一侧）。

### 4.4 步骤 1.4 — certified 变体 + 受控对照

- **目标**：补 **certified 变体**——RBC（echo / ready 两阶段）+ 2f+1 证书；区块被引用前可用性已保证。
- **实现要点**：替换可插拔的认证模块即得 certified 版；除该模块外配置与 uncertified 版逐项相同，可一键切换，构成受控 minimal pair。
- **验收**：两变体除认证模块外配置完全一致；切换不影响骨架行为。
- **论文锚点**：§3.3 受控 minimal-pair、§3.5（论文称现实中不可构造——仿真器中可构造）、§5.5 提交路径对照。

### 4.5 步骤 1.5 — 拜占庭敌手（先只建等价攻击）

- **目标**：实现**等价（equivocation）攻击**（决策 1）——拜占庭作者在同一 (author, round) 槽位向不同验证者发不同区块。
- **实现要点**：敌手策略接口预留扩展位；拜占庭比例可调（0 ~ f）。"引用不给内容""晚绑定排序操纵"留作后续迭代，不在本步实现。
- **验收**：等价攻击能在 uncertified 变体触发提交规则的等价中和逻辑；能观测到对槽位唯一性的破坏。
- **论文锚点**：§6.2 槽位唯一性破坏、§5.2 非等价性的事后中和。

### 4.6 步骤 1.6 — 测量层

- **目标**：从运行中测量而非假定 Δ_save（每轮节省的消息延迟）、Δ_recover（单次 reconciliation 实测额外延迟，含排队）、ρ（补偿机制实测触发频率）。
- **实现要点**：逐轮埋点，输出按不变量与情形分解的成本台账（对照论文 §7.1 记账方法）；每个输出量标注 §2.4 映射表行号。
- **验收**：能导出每次运行的成本台账 CSV/JSON，字段含回填追溯标记。
- **论文锚点**：§7.1 成本台账方法、§7.2 成本模型。

### 4.7 步骤 1.7 — 扫参与出图

- **目标**：在 (ρ, Δ_recover) 参数空间扫参，产出 **Figure 1/2 的实测版**。
- **实现要点**：网格扫参 + 多 trial；产出净优势曲线与热力图，叠加论文解析曲线对照。
- **验收**：生成与论文 Figure 1/2 可并排比较的图；标注"仿真实测"。
- **论文锚点**：§7.2 图 1、§7.6 图 2、§10 未来工作之二。

### 4.8 步骤 1.8 — 验证与回归

- **目标**：确立仿真器正确性。
- **实现要点**：
  - 退化检验——关闭排队/耦合后，仿真均值应收敛到闭式 `Δ_save − ρ·Δ_recover`（相对误差阈值，如 <1%）；
  - 耦合检验——开启拥塞反馈后，应观测到对解析曲线的系统性偏离（§7.4 预测）；
  - 与 `dag_bft_model.py` 的 Monte Carlo 结果交叉核对。
- **验收**：退化检验通过；耦合偏离方向与论文定性预测一致。
- **论文锚点**：§7.2、§7.4、§9。

### 4.9 阶段一交付物

1. `stage6_simulation/` 仿真器源码（骨架 + uncertified/certified 两变体 + 等价攻击敌手 + 测量层）；
2. 扫参脚本与结果数据（带回填追溯标记的成本台账）；
3. Figure 1/2 实测版及与解析模型的对照图；
4. `stage6_simulation/README.md`——运行说明 + 诚实声明；
5. **回填材料包**：一份《阶段一发现摘要》，逐条对照 §2.4 映射表，说明每项结果回填论文哪一节、改动文字建议——直接作为阶段三输入。

### 4.10 阶段一里程碑

- M1：骨架可跑通（1.1–1.2）。
- M2：uncertified 变体跑通（1.3）。
- M3：certified 变体 + 受控对照 + 等价攻击就绪（1.4–1.5）。
- M4：测量 + 扫参 + 出图（1.6–1.7）。
- M5：验证通过、回填材料包成文（1.8 + 交付物）。

---

## 5. 阶段二：真实部署（渐进准备）

### 5.0 概述

仿真稳定（M5 达成）后启动。目标：在本地跑真实 DAG-BFT 协议，得到实测数据校准阶段一仿真，并为论文提供"贴合真实协议"的证据。

### 5.1 步骤 2.1 — 选定目标代码库

- Bullshark/Narwhal 与 Mysticeti 均在 MystenLabs `sui` 仓库（Rust）；另有 mysticeti 独立基准脚本、narwhal benchmark 脚本。
- 产出：目标代码库清单 + 构建可行性评估。

### 5.2 步骤 2.2 — 本地多节点 testnet

- 用 Docker Compose 编排 N 个 validator 容器，组成本地委员会。
- **届时安装 skill**：`manutej/luxor-claude-marketplace@docker-compose-orchestration`。

### 5.3 步骤 2.3 — 故障注入与延迟测量

- netem/tc 注入网络抖动与延迟，kill 节点制造故障；在 Rust 共识代码中埋点测延迟。
- **届时安装 skill**：`wshobson/agents@rust-async-patterns`（读懂/魔改 Rust 异步共识代码）。

### 5.4 步骤 2.4 — 仿真↔实测对照

- 用真实实测的延迟分布、恢复成本校准阶段一仿真（含 4.1 的延迟分布参数）；
- 产出论文 §10 未来工作之二所要求的"跨协议受控实证"雏形。

### 5.5 阶段二交付物

1. 本地多节点 testnet 的 Docker 编排配置；
2. 故障注入脚本与实测延迟数据；
3. 仿真预测 vs 真实实测对照报告；
4. **回填材料包**：《阶段二发现摘要》，对照 §2.4 映射表，给出真实实测结果的论文回填建议。

### 5.6 诚实边界

真实部署只能测到**混淆后的总效果**（§3.5）——真实 uncertified ≠ 某协议减认证。因此阶段二**不能**独立隔离"认证"这一变量；其作用是给阶段一仿真提供真实世界的延迟/成本锚点，并为论文提供真实协议层面的佐证；受控对照始终由阶段一承担。

---

## 6. 阶段三：结果回填论文

### 6.0 概述与审批关卡

把阶段一、二的回填材料包系统性地写入论文。**增量进行**：阶段一 M5 后做第一轮回填（仿真结果），阶段二完成后做第二轮（实测结果）。

> ⚠️ **硬性审批关卡（用户要求）**：阶段三**不得自动启动**。每一轮回填前须停下来，向用户呈递回填材料包（§4.9 / §5.5 的发现摘要 + 拟改动清单），**取得用户明确批准后**才动笔。在获批之前：
> - 不修改 `final/final_paper.md`；
> - 不在 `stage8_paper_integration/` 生成任何修订稿；
> - 仅允许准备呈递材料（发现摘要、§2.4 映射表对照、拟改动文字草案）。
>
> 增量回填意味着该关卡至少触发两次（阶段一后、阶段二后），每次独立审批。

### 6.1 回填范围

依 §2.4 映射表逐节修订，重点：
- §3.5 / §5.5：受控 minimal pair 由"无法构造"补注为"已在仿真中构造";
- §7.2 / §7.6：增配实测版 Figure 1/2，或改为解析+实测对照图；
- §7.4 / §9：排队与耦合效应的局限条目，从"未刻画"改写为"已定量刻画";
- §7.7：边界情形 4 若获仿真结论则改写;
- §7.1–7.2：成本台账参数升级为标定值;
- §10：未来工作之二部分完成，相应上移;
- §9 数据可用性声明、AI 使用声明、摘要：同步论文性质变化。

### 6.2 论文性质变化与新增结构

回填后论文从"纯理论分析型"变为"理论分析 + 受控实证"。需决策（阶段三启动时确认）：
- **方案 A**：将实证结果分散写入既有 §7、§9、§10；
- **方案 B**：新增一章（如"第 X 章 仿真验证与实证标定"）集中承载，既有章节仅加交叉引用。
- 文档倾向：仿真内容体量大时用方案 B，便于保持原论证主线清晰。

### 6.3 回填工具与流程

- 论文由 academic-research-skills（ARS）流水线产出，回填修订建议走 ARS 的 **`ars-revision`** 模式（revised draft + 修订说明），保持与原流水线一致的风格与质量门禁;
- 修订须经一轮一致性核查：新增实证内容不得与论文 §1.4 诚实声明、抗断章框架冲突。

### 6.4 阶段三交付物

1. 回填后的论文修订稿（`stage8_paper_integration/` 下，保留 `final/final_paper.md` 原稿不动）；
2. 修订说明：逐条对应 §2.4 映射表，说明改了什么、依据哪项实证结果;
3. 更新后的图表包与数据可用性声明。

---

## 7. 目录结构（规划）

```
Blockchain_final_pipeline/
├── 仿真与部署_实施计划.md         # 本文档
├── final/ stage1.../ ...           # 论文流水线既有产物（不改动）
├── stage2_write/code/              # 既有闭式模型（不改动，作回归基线）
├── stage6_simulation/              # 阶段一：SimPy 仿真器
│   ├── README.md
│   ├── core/                       # 骨架：验证者、轮次、信道、延迟分布
│   ├── invariants/                 # 三个不变量模块（按论文 §3.2 切分）
│   ├── variants/                   # uncertified / certified 两变体
│   ├── adversary/                  # 拜占庭敌手（先等价攻击）
│   ├── metrics/                    # 测量层 + 成本台账
│   ├── experiments/                # 扫参脚本
│   ├── data/                       # 原始结果数据 + manifest（即时留存，§2.5）
│   └── figures/                    # 实测版图表（.png 预览 + .pdf 300DPI）
├── stage7_deployment/              # 阶段二：真实部署（后续创建；含 data/、figures/）
└── stage8_paper_integration/       # 阶段三：论文回填修订稿（后续创建）
```

---

## 8. Skills 清单

| Skill | 用途 | 状态 |
|---|---|---|
| `davila7/claude-code-templates@simpy` | 阶段一离散事件仿真 | ✅ 已安装 |
| `manutej/luxor-claude-marketplace@docker-compose-orchestration` | 阶段二本地多节点编排 | 待装（步骤 2.2） |
| `wshobson/agents@rust-async-patterns` | 阶段二读懂/魔改 Rust 共识代码 | 待装（步骤 2.3） |
| ARS `ars-revision`（已加载插件） | 阶段三论文回填修订 | 已可用 |

---

## 9. 风险与诚实边界

| 风险 | 应对 |
|---|---|
| 仿真器本身成为新的"未受控比较" | 严格执行受控 minimal pair（4.4）+ 退化回归（4.8） |
| 仿真参数取值缺乏现实依据 | 阶段二真实实测校准；在此之前所有数值标注为"说明性" |
| 真实部署混淆变量（§3.5） | 明确阶段二只作校准/佐证、不作受控隔离 |
| 范围蔓延（实现完整协议） | 仿真器只需覆盖论文语料库 C 的共性结构，不追求协议级完整复现 |
| 回填越界宣称 | 守住 §2.4 两个诚实边界：阶段一是"实现核对"，仅阶段二是"贴合真实协议"的证据 |
| 回填破坏论文原论证 | 阶段三经一致性核查，保留 §1.4 诚实声明与抗断章框架 |
| 结果无法回填而白做 | 设计原则 6：每个输出量须先对应 §2.4 映射表某行，否则不产出 |

---

## 10. 立即下一步

从**步骤 1.1（仿真骨架）**起步：在 `stage6_simulation/` 搭建验证者进程、轮次推进、消息信道与**指定分布的网络延迟**。三个设计决策已确认（§0.1），可直接开工。

后续顺序：1.1 骨架 → 1.2 排队资源 → 1.3 uncertified 变体跑通 → 1.4 certified 变体 + 受控对照 → 1.5 等价攻击 → 1.6 测量 → 1.7 扫参出图 → 1.8 验证 → 阶段一回填材料包。
