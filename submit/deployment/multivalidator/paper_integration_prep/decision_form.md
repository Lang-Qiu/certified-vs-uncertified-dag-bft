# 阶段三回填决策表(待用户填写)

> 用户可以在回复中以文字简述选择,无需复制本表。本表仅为参考结构。

## 一、整体决策

- [ ] **批准**进入阶段三回填(随后将创建 `stage8_paper_integration/` 并对照 `final/final_paper.md` 做最小变动 diff)
- [ ] **暂缓**,等待补测/复盘后再决定
- [ ] **拒绝**(请说明理由,以便记入决策日志)

## 二、候选回填点(任意子集勾选)

- [ ] RP-1 跨规模部署实证 → §7.6.1
- [ ] RP-2 故障可加性边界 → §8.4
- [ ] RP-3 实证仪器局限 → §9

## 三、补测决策

- [ ] 立即回填,先用单种子数据,后续补独立种子作为修订
- [ ] **先补独立种子**,再决定是否回填(成本估计:n=7 5% 丢包 +1 种子 ~30 min;n=4 ablation +1 种子 ~2.5 h)
- [ ] **先补 G3「at/above f × 高强度」象限**(本会话进行中,预计 ~1 h),让两轴稳定性图完整

## 四、回填颗粒度

- [ ] 最小侵入(仅追加,不修改原文判断)— **草稿当前为此方案**
- [ ] 中度修订(允许在 §7.6 表格末尾或摘要中改述一两句)
- [ ] 重写式回填(§7.6 改为「理论 + 实证」双轨结构,语气大幅调整)

## 五、若批准,后续动作

1. 创建 `stage8_paper_integration/` 目录;
2. 对每个被批准的 RP 生成一份「最小 diff 修订稿」(以 `git diff`-like 格式呈现),供二次确认;
3. 二次确认通过后,Claude 才编辑 `final/final_paper.md`;
4. 编辑后立即跑 integrity verification (字数、引用计数、未引断言扫描),输出 verification 报告;
5. 生成阶段三总结记录,落到 `memory/` 作为后续工作锚点。

---

生成时间:2026-05-25
本表与 [[paper-integration-approval-gate]] 用户规则严格一致——批准前 Claude 不会动 `final/final_paper.md` 或创建 `stage8_paper_integration/`。
